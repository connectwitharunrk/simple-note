<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain HTTP by default, which would make every request to a local development
  backend fail.

  Cleartext is re-enabled only for the specific hosts used during development, rather than
  globally with android:usesCleartextTraffic="true". That keeps the production default intact:
  if this app is later pointed at a real server, HTTPS is still required and a misconfigured
  http:// URL fails loudly instead of silently sending notes in the clear.

  10.0.2.2 is the emulator's alias for the host machine's loopback interface.
  To use a physical device against a backend on your LAN, add that machine's IP here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
